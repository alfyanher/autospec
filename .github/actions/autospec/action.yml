name: 'AutoSpec'
description: 'Generate living documentation using Claude Code'
author: 'Alfonso Yanez Herrera'

branding:
  icon: 'book-open'
  color: 'blue'

inputs:
  anthropic-api-key:
    description: 'Anthropic API key for Claude Code'
    required: true
  mode:
    description: 'Generation mode: full | changed'
    default: 'changed'
  docs:
    description: 'Comma-separated list of docs to generate (default: all)'
    default: 'all'
  commit:
    description: 'Auto-commit generated docs (true/false)'
    default: 'true'
  tone:
    description: 'Documentation tone: concise | detailed | enterprise'
    default: 'concise'

outputs:
  generated:
    description: 'Comma-separated list of generated document filenames'
  changed:
    description: 'Whether any docs were updated (true/false)'

runs:
  using: 'composite'
  steps:
    - name: Install tools
      shell: bash
      run: npm install -g autospec @anthropic-ai/claude-code

    - name: Run AutoSpec
      shell: bash
      env:
        ANTHROPIC_API_KEY: ${{ inputs.anthropic-api-key }}
      run: |
        # Init if no config exists
        if [ ! -f ".autospec.yaml" ]; then
          autospec init --no-hooks --quiet
        fi

        ARGS="--quiet"
        if [ "${{ inputs.mode }}" = "changed" ]; then
          ARGS="$ARGS --changed"
        fi
        if [ "${{ inputs.docs }}" != "all" ]; then
          ARGS="$ARGS --docs ${{ inputs.docs }}"
        fi
        if [ "${{ inputs.tone }}" != "concise" ]; then
          ARGS="$ARGS --tone ${{ inputs.tone }}"
        fi

        autospec generate $ARGS

    - name: Check for changes and commit
      shell: bash
      if: inputs.commit == 'true'
      run: |
        if git diff --quiet .autospec/ CLAUDE.md 2>/dev/null; then
          echo "changed=false" >> $GITHUB_OUTPUT
        else
          echo "changed=true" >> $GITHUB_OUTPUT
          git config user.name "AutoSpec Bot"
          git config user.email "autospec[bot]@users.noreply.github.com"
          git add .autospec/ CLAUDE.md
          git commit -m "docs(autospec): update living documentation [skip ci]"
          git push
        fi
